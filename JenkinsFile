pipeline {
  agent any
  
  stages {
    stage('Build') {
      steps {
        script {
          git branch: 'Produit', credentials: env.GITHUB_CREDENTIALS, url: 'https://github.com/rayenbac/PI-nest'
          bat 'npm install'
        }
      }
    }
  }
}